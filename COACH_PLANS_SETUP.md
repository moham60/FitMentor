# Coach Plans Setup Guide

## Database Migration

تم إنشاء جداول قاعدة البيانات للخطط التدريبية الخاصة بالمدربين.

### الجداول المُنشأة:

1. **coach_plans** - جدول الخطط التدريبية
   - id: معرّف فريد للخطة
   - coach_id: معرّف المدرب (مرتبط بجدول profiles)
   - name: اسم الخطة
   - type: نوع الخطة (basic, gold, premium)
   - description: وصف الخطة (اختياري)
   - price: سعر الخطة (اختياري)
   - created_at, updated_at: تواريخ الإنشاء والتحديث

2. **plan_muscles** - جدول العضلات المرتبطة بكل خطة
   - id: معرّف فريد
   - plan_id: معرّف الخطة
   - muscle_id: معرّف العضلة
   - muscle_name: اسم العضلة
   - exercise_count: عدد التمارين
   - sets: عدد المجموعات
   - reps: عدد التكرارات
   - order_index: ترتيب العضلة في الخطة

### كيفية تطبيق Migration:

#### الطريقة 1: استخدام Supabase CLI (موصى بها)
```bash
# تأكد من أنك في مجلد المشروع
cd d:\final-project\fitmentor

# تطبيق المigration
npx supabase db push
```

#### الطريقة 2: من لوحة تحكم Supabase
1. افتح مشروعك في Supabase Dashboard
2. اذهب إلى SQL Editor
3. افتح الملف `supabase/migrations/20260116130000_coach_plans.sql`
4. انسخ المحتوى وألصقه في SQL Editor
5. اضغط Run

### الميزات:

✅ كل مدرب يستطيع إنشاء خططه الخاصة
✅ يتم حفظ الخطط في قاعدة البيانات
✅ دعم أنواع مختلفة من الخطط (Basic, Gold, Premium)
✅ إمكانية إضافة عدة مجموعات عضلية لكل خطة
✅ حماية البيانات بـ Row Level Security (RLS)
✅ كل مدرب يرى خططه فقط

### Security (RLS Policies):

تم تطبيق سياسات الأمان التالية:
- المدربون يستطيعون عرض خططهم فقط
- المدربون يستطيعون إنشاء خطط جديدة
- المدربون يستطيعون تحديث وحذف خططهم فقط
- حذف الخطة يحذف تلقائياً جميع العضلات المرتبطة بها

### استخدام الواجهة:

1. انتقل إلى صفحة Plans
2. اضغط "Create New Plan"
3. أدخل اسم الخطة واختر النوع
4. اضغط على العضلات من الخريطة لإضافتها
5. قم بتعديل عدد التمارين والمجموعات والتكرارات لكل عضلة
6. اضغط "Save Final Plan"
